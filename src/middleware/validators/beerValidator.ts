// import { body } from "express-validator";

// export const validateBeerCreation = [
// 	body("name").notEmpty().trim(),
// 	body("description").notEmpty(),
// 	body("abv").isFloat({ min: 0, max: 100 }),
// 	body("brewery_id").isInt(),
// 	body("category_id").isInt(),
// ];
